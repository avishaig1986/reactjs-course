// to initialize the snippets:
// ctrl + alt + r and select rafce

import React from "react";

const LineItem = ({ title }) => {
  return (
    <div class="col-sm 
    text-center
    bg-dark 
    text-white">
 {title}
</div>
  );
};

export default LineItem;
